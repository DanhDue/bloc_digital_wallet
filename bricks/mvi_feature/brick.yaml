name: mvi_feature
description: "[DEPRECATED — use pac_mvi_feature/pac_mvi_subfeature instead] MVI feature with BaseMviPage - Initial state only, skeleton layers for expansion"
version: 1.0.0+1

environment:
  mason: ^0.1.2

vars:
  feature_name:
    type: string
    description: The name of the feature (e.g., dashboard, settings)
    prompt: What is the feature name?
