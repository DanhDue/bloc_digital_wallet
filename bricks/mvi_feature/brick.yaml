name: mvi_feature
description: Generate a feature following Clean Architecture + MVI pattern with feature-first organization
version: 1.0.0+1

environment:
  mason: ^0.1.2

vars:
  feature_name:
    type: string
    description: The name of the feature (e.g., authentication, wallet, profile)
    prompt: What is the feature name?