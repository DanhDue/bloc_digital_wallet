name: {{name.snakeCase()}}
description: "{{name.pascalCase()}} native plugin for Super App."
version: 0.0.1
resolution: workspace
publish_to: none

environment:
  sdk: ^3.10.7
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
{{^has_ui}}
  pigeon: 26.3.2
{{/has_ui}}

flutter:
  plugin:
    platforms:
      android:
        package: com.danhdue.{{name.snakeCase()}}
        pluginClass: {{name.pascalCase()}}Plugin
      ios:
        pluginClass: {{name.pascalCase()}}Plugin
