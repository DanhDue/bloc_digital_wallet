name: {{name.snakeCase()}}
description: "{{name.pascalCase()}} shared library for Super App."
version: 0.0.1
resolution: workspace
publish_to: none

environment:
  sdk: ^3.10.7
{{#is_flutter}}
dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
{{/is_flutter}}
{{^is_flutter}}
dependencies:

dev_dependencies:
  test: ^1.25.2
  lints: ^6.0.0
{{/is_flutter}}
