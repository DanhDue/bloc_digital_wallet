import 'package:flutter_test/flutter_test.dart';

void main() {
  test('logger package can be imported', () {
    // TODO(logging-refactor): Add tests when interfaces are defined
    expect(true, true);
  });
}
