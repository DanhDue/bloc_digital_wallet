/// Logger package - Foundation for the logging refactor epic.
///
/// This is an empty pure-Dart package that will serve as the foundation for
/// the logging interfaces and implementations across the workspace.
/// Concrete logging backends will be added in subsequent tasks.
library;
