name: network
description: Network layer for the Bloc Digital Wallet using Dio and Retrofit.
version: 0.1.0
resolution: workspace
publish_to: none

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  native_security:
    path: ../native_security
  logger:
    path: ../logger
  # Network-specific dependencies
  json_annotation: ^4.9.0
  crypto: ^3.0.3
  dio: ^5.9.0
  get_it: ^9.2.0
  injectable: ^2.5.0
  talker_flutter: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.10.4
  json_serializable: ^6.8.0
  injectable_generator: ^2.6.1

flutter:
