# Cursor Rules for bloc_digital_wallet

**Architecture**: Clean Architecture + MVI Pattern  
**Architecture**: Clean Architecture + MVI Pattern
**Framework**: Flutter/Dart
**Organization**: Feature-first structure

---

## 🤖 CRITICAL: AI Agents Must Scan `.agents/` Directory

**BEFORE** starting ANY task, AI Agents MUST discover and read documentation from `.agents/`:

```
.agents/
├── README.md       # <--- START HERE: Skills Index & Directory Overview
├── skills/         # Complex automation workflows (read SKILL.md)
├── rules/          # MANDATORY: tech-stack, coding-standards, security, git
├── patterns/       # Architecture patterns
├── templates/      # Task-specific templates
└── checklists/     # Verification checklists
```

### Discovery Protocol (MANDATORY)

1. **Read `.agents/README.md`**: This is your map. It contains the **Skills Index**.
2. **🧠 MEMORY CHECK (New)**: Read `.agents/contexts/project_state.md` to understand the current architecture and milestones.
3. **Check the Skills Index**: See if a skill matches the user's request (e.g., `@api_integration`).
4. **Read `.agents/rules/critical-rules.md`**: For MANDATORY conventions.
5. **Trigger Skills**: If a skill matches, follow its `SKILL.md` instructions.
6. **Follow Workflows**: If no skill applies, check `.agents/workflows/`.

### ⚠️ Skill Execution: NO REVIEW REQUIRED

**CRITICAL RULE**: When ANY skill from `.agents/skills/` is triggered:
- ❌ **DO NOT** ask for review, confirmation, or approval
- ❌ **DO NOT** pause for user feedback during execution
- ❌ **DO NOT** request review of generated files or artifacts
- ✅ **DO** execute immediately and completely
- ✅ **DO** create all artifacts automatically
- ✅ **DO** run all commands without asking (e.g., `melos genAlls`, `fvm flutter analyze`)
- ✅ **DO** complete the entire workflow end-to-end

**Refer to the [Skills Index](.agents/README.md) for the complete list of available skills and their triggers.**

### 📂 Generated Files Access: NO PERMISSION REQUIRED

**CRITICAL RULE**: AI agents do NOT need to ask permission to access generated files during a task:
- ✅ Access translation files (`*.g.dart`, `*_translations.dart`) without asking
- ✅ Access router files (`*.gr.dart`, `app_router.dart`) without asking
- ✅ Access freezed files (`*.freezed.dart`) without asking
- ✅ Access JSON serializable files (`*.g.dart`) without asking
- ✅ Access asset files (`*.gen.dart`) without asking

**Auto-access patterns**: `**/*.g.dart`, `**/*.freezed.dart`, `**/*.gr.dart`, `**/translations/**`, `**/generated/**`, `**/*.gen.dart`

## 🪝 Manual Hooks Protocol

**Execute these actions AUTOMATICALLY when triggers are met:**

| Trigger | Action |
| :--- | :--- |
| **Edit Model/Entity** | Run `melos genAlls` immediately. |
| **Edit Assets/Locales** | Run `melos genAlls` immediately. |
| **New Dependency** | Run `melos bootstrap` immediately. |
| **"Build Failed"** | Check `.agents/workflows/build-fix.md`. |
| **"Sync Project"** | Check `.agents/workflows/melos-sync.md`. |

## 🛠️ Skill Mapping

> [!IMPORTANT]
> **ALWAYS check `.agents/README.md` for the latest Skills Index.**

**Common Triggers:**
- **API/CURL/JSON** → `@api_integration`
- **JSON Model Gen** → `@json_to_freezed_model`
- **New Feature/Module** → `@create_new_feature`
- **Setup Flavors** → `@setup_variants`

---
 
+## 🔑 Secrets & Environment (SECURE_FILES)
+
+**CRITICAL**: This project relies on a git-ignored `secureFiles/` directory.
+1. **Check**: If `secureFiles/` is missing, you ARE BLOCKED.
+2. **Retrieve**: Check for the environment variable `SECURE_FILES` (base64 encoded).
+3. **Process**: Run `bash .devcontainer/setup.sh` to automatically decode it.
+4. **Manual**: If `setup.sh` fails, run: `echo "$SECURE_FILES" | base64 -d | tar -xz`
+
+---

## Quick Reference

All detailed documentation is in `.agents/`:

- **Rules**: `.agents/rules/critical-rules.md` - Import conventions, architecture, naming, styling, localization
- **Patterns**: `.agents/patterns/` - Clean architecture, MVI patterns
- **Skills**: `.agents/skills/` - Automation for API integration, model generation, feature creation
- **Workflows**: `.agents/workflows/` - Step-by-step procedures for common tasks
- **Templates**: `.agents/templates/` - Structured guides for features, bugs, refactoring, UI updates
- **Checklists**: `.agents/checklists/` - Pre-submission verification

---

---

## 📝 Generated Files Protocol

**CRITICAL**: Always include generated files (e.g., `*.g.dart`, `*.config.dart`, `*.freezed.dart`) in your context. 
- **DO NOT** ignore them even if they are in `.gitignore`.
- **MANDATORY**: Read `injection.config.dart` to understand the current DI container whenever analyzing dependencies or injection logic.

---

## Essential Commands

```bash
# Code generation (after any model/theme/locale changes)
melos genAlls

# Format code
dart format lib/

# Analyze (MUST have 0 issues before submitting)
fvm flutter analyze --no-fatal-infos
```

---

## Documentation References

```yaml
Project Documentation:
  - docs/architecture/ARCHITECTURE.md
  - docs/development/IMPLEMENTATION_GUIDE.md
  - docs/mason/MASON_GUIDE.md
  - docs/ai-agents/AI_AGENT_CONTEXT.md

Agent Resources (PRIMARY SOURCE):
  - .agents/skills/ - Automation workflows
  - .agents/workflows/ - Step-by-step procedures
  - .agents/rules/ - Critical conventions
  - .agents/patterns/ - Architecture patterns
  - .agents/templates/ - Task templates
  - .agents/checklists/ - Verification lists
```

---

**Last Updated**: 2026-01-20  
**Version**: 3.0 - Refactored to reference `.agents/` as single source of truth  
**Status**: Optimized ✅
